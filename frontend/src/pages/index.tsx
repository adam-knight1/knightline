import { useState, changeEvent, FormEvent } from 'react';

interface LoginData {
    username: string;
    password: string;
    }

interface Registration {
    username: string;
    password: string;
    email: string;
}